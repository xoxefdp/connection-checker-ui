{
  "name": "connection-checker-ui",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "clean": "rimraf dist",
    "prestart": "npm run clean",
    "start": "npx electron --disable-gpu src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "connection-checker": "^1.4.0",
    "electron": "^11.1.1",
    "timer-creator": "^1.0.1"
  },
  "devDependencies": {
    "the-browser-logger": "^1.0.4",
    "rimraf": "^3.0.2"
  }
}
